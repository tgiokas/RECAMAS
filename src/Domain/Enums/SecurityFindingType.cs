namespace RECAMAS.Domain.Enums;

/// Specs Table 36 (Case-level Security Checks) — reused here for the profile-level equivalent (Table 13).
public enum SecurityFindingType
{
    CriminalRecord = 1,
    RestrictiveActivity = 2,
    Other = 3,
}
